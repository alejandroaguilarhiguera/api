import { string, object } from '@hapi/joi';

export default object().unknown().keys({
  NODE_ENV: string().required(),
  SERVER_PORT: string().required(),
  API_URL: string().required(),
  API_PREFIX: string().required(),
  API_UPLOADS_PATH: string().required(),
  API_STATICS_PATH: string().required(),
  API_STATICS_URL: string().required(),
  API_DOMAIN: string().required(),
  API_SECURITY_SALT: string().required(),
  API_SECURITY_CYPHER: string().required(),
  API_REFRESH_TOKEN_EXPIRES_IN: string().required(),
  API_EMAIL_NOREPLY: string().required(),
  APP_URL: string().required(),
  MYSQL_HOST: string().required(),
  MYSQL_PORT: string().required(),
  MYSQL_DATABASE: string().required(),
  MYSQL_USER: string().required(),
  MYSQL_PASSWORD: string().required(),
  API_UPLOADS_URL: string().required(),
  GCP_PROJECT_ID: string().required(),
  GCP_STORAGE_BUCKET_NAME: string().required(),
  GCP_CREDENTIALS_TYPE: string().required(),
  GCP_CREDENTIALS_KEY_ID: string().required(),
  GCP_CREDENTIALS_KEY: string().required(),
  GCP_CREDENTIALS_CLIENT_EMAIL: string().required(),
  GCP_CREDENTIALS_CLIENT_ID: string().required(),
  GCP_CREDENTIALS_AUTH_URI: string().required(),
  GCP_CREDENTIALS_TOKEN_URI: string().required(),
  GCP_CREDENTIALS_AUTH_PROVIDER_X509_CERT_URL: string().required(),
  GCP_CREDENTIALS_CLIENT_X509_CERT_URL: string().required(),
  SMTP_HOST: string().required(),
  SMTP_PORT: string().required(),
  SMTP_USER: string().required(),
  SMTP_PASS: string().required(),
  FIREBASE_ENABLE: string().required(),
  FIREBASE_API_KEY: string().required(),
  FIREBASE_AUTH_DOMAIN: string().required(),
  FIREBASE_DB: string().required(),
  FIREBASE_PROJECT_ID: string().required(),
  FIREBASE_BUCKET: string().required(),
  FIREBASE_MESSAGING_ID: string().required(),
  FIREBASE_APP_ID: string().required(),
  FIREBASE_PRIVATE_KEY: string().required(),
  FIREBASE_CLIENT_EMAIL: string().required(),
  FIREBASE_PREFIX: string().required(),
  TEST_EMAIL: string().email().required(),
  TEST_PASSWORD: string().required(),
  CRONTAB_PASS: string().required(),
  SUPER_USER: string().email().required(),
  ADMIN_USER: string().email().required(),
  CRON_USER: string().email().required(),
  DISABLED_USER: string().email().required(),
  UNCONFIRMED_USER: string().email().required(),
  PUBLIC_USER: string().email().required(),
  CUSTOMER_USER: string().email().required(),
  SHOP_OWNER_USER: string().email().required(),
  STAFF_USER: string().email().required(),
  FIREBASE_NOTIFICATIONS: string().required(),
  GITHUB_CLIENT_ID: string().required(),
  GITHUB_SECRET_ID: string().required(),
  GITLAB_CLIENT_ID: string().required(),
  GITLAB_SECRET_ID: string().required(),
  BITBUCKET_CLIENT_ID: string().required(),
  BITBUCKET_SECRET_ID: string().required(),
  TEST_PHONE: string().required(),
  STRIPE_SECRET_KEY: string().required(),
  STRIPE_PUBLIC_KEY: string().required(),
});
